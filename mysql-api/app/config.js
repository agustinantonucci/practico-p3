const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "localhost",
    user: "userTP",
    password: "1234",
    database: "TP",
  },
  listPerPage: 10,
};
module.exports = config
